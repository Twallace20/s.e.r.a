# Do Not Migrate

Do not migrate:

- generated `.sera-*` reports
- old run artifacts
- sample media unless used as fixtures
- stale backups
- placeholder PWA/Lite code
- broken CJS/JS experiments
- package scripts that are not actively proven
- future certs as active tests
- README/status text that is not generated from current certs

Do not migrate anything just because it exists.
