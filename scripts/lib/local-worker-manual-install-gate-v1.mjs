import fs from "node:fs";
import path from "node:path";

const declaredPaths = [
  "docs/phases/PHASE_70_LOCAL_WORKER_MANUAL_INSTALL_GATE_V1.md",
  "scripts/lib/local-worker-manual-install-gate-v1.mjs",
  "scripts/run-local-worker-manual-install-gate-v1.mjs",
  "tests/integration/local-worker-manual-install-gate-v1.test.ts",
  "apps/operator-console/src/local-worker-manual-install-gate.ts"
];
const manualInstallGateRequirements = [
  {"id": "phase-69-install-evidence-packet-reviewed", "label": "Phase 69 install evidence packet reviewed", "state": "required", "evidence": "Phase 69 install evidence packet proof must be reviewed before manual install gate review."},
  {"id": "explicit-manual-install-gate-required", "label": "Explicit manual install gate required", "state": "required", "evidence": "The owner must have a separate manual install gate before any future install can be considered."},
  {"id": "owner-install-review-required", "label": "Owner install review required", "state": "required", "evidence": "Tyler must review the install gate as an owner action, not an automated action."},
  {"id": "manual-install-command-plan-required", "label": "Manual install command plan required", "state": "required", "evidence": "Any future install command plan must be listed for review while commands remain blocked in this phase."},
  {"id": "final-preinstall-checklist-required", "label": "Final preinstall checklist required", "state": "required", "evidence": "The preinstall checklist must remain a review artifact and not an approval record."},
  {"id": "install-remains-blocked-required", "label": "Install remains blocked required", "state": "required", "evidence": "The gate must prove install, execution, connectivity, scheduler, and persistence remain blocked."}
];
const manualInstallGateFields = [
  "owner",
  "manualInstallGateId",
  "sourcePhase",
  "safeState",
  "phase69InstallEvidencePacketReady",
  "explicitManualInstallGateRequired",
  "manualInstallGateLocked",
  "workerInstallApproved"
];
const manualInstallGateSignals = [
  "manual install gate surface",
  "phase 69 install evidence packet dependency",
  "explicit manual gate requirement",
  "owner install review requirement",
  "manual install command plan requirement",
  "final preinstall checklist requirement",
  "manual install gate not locked signal",
  "actual install remains blocked"
];
const evidenceRequirements = [
  "Phase 69 install evidence packet proof",
  "Manual install gate requirement",
  "Owner install review requirement",
  "Manual install command plan requirement",
  "Final preinstall checklist requirement",
  "Blocked install proof"
];
const safetyGates = [
  "Local worker manual install gate only",
  "Tyler remains the manual install gate owner",
  "Manual install gate is declarative only",
  "Manual install gate prepares owner review without approving installation",
  "Manual install gate does not sign approval",
  "Manual install gate does not lock itself as approved",
  "Manual install gate does not download dependencies",
  "Manual install gate does not install packages",
  "Manual install gate does not run package managers",
  "Manual install gate does not execute installers",
  "Manual install gate does not create lockfiles",
  "Manual install gate does not mutate dependency manifests",
  "Manual install gate does not run smoke tests",
  "Manual install gate does not create install artifacts outside its local report folder",
  "Manual install gate does not mark the worker ready for install",
  "Manual install gate does not approve the install evidence packet",
  "Manual install gate does not approve the dependency allowlist",
  "Manual install gate does not approve the rollback plan",
  "Manual install gate does not approve the workspace boundary",
  "Manual install gate does not approve the scope lock",
  "Manual install gate does not approve the install approval record",
  "Manual install gate does not approve the install plan",
  "Manual install gate does not approve worker installation",
  "Manual install gate does not approve execution",
  "Manual install gate does not authorize overnight work",
  "Manual install gate does not install a worker",
  "Manual install gate does not create files",
  "Manual install gate does not mutate files",
  "Manual install gate does not mutate source",
  "Manual install gate does not mutate the filesystem",
  "Manual install gate does not connect to a worker",
  "Manual install gate does not start a worker",
  "Manual install gate does not spawn a worker process",
  "Manual install gate does not poll worker health",
  "Manual install gate does not inspect running processes",
  "Manual install gate does not create scheduled tasks",
  "Manual install gate does not modify scheduled tasks",
  "Manual install gate does not delete scheduled tasks",
  "Manual install gate does not enable scheduled tasks",
  "Manual install gate does not disable scheduled tasks",
  "Manual install gate does not query Windows Task Scheduler",
  "Manual install gate does not run scheduled tasks",
  "Manual install gate does not execute PowerShell",
  "Manual install gate does not execute schtasks",
  "Manual install gate does not execute commands",
  "Manual install gate does not execute shell commands",
  "Manual install gate does not execute tasks",
  "Manual install gate does not persist task records",
  "Manual install gate does not persist owner records",
  "Manual install gate does not persist unlock proposal decisions",
  "Manual install gate does not persist install plan decisions",
  "Manual install gate does not persist approval records",
  "Manual install gate does not persist scope lock records",
  "Manual install gate does not persist workspace boundary records",
  "Manual install gate does not persist rollback records",
  "Manual install gate does not persist dependency allowlist records",
  "Manual install gate does not persist install dry-run records",
  "Manual install gate does not persist install evidence packet records",
  "Manual install gate does not persist manual install gate records",
  "Manual install gate does not connect to runner infrastructure",
  "Manual install gate does not route work",
  "Manual install gate does not process work automatically",
  "Manual install gate does not merge branches",
  "Manual install gate cannot self-approve",
  "Phase 69 install evidence packet prerequisite remains represented",
  "Phase 68 install dry-run prerequisite remains represented",
  "Owner approval is required before any future install",
  "Manual review is required before any future install",
  "Explicit manual install gate is required before any future install",
  "Owner install review is required before any future install",
  "Manual install command plan is required before any future install",
  "Final preinstall checklist is required before any future install",
  "Install remains blocked by design",
  "Emergency stop acknowledgement is required before any future install",
  "Local worker ready for install remains false by design",
  "Manual install gate locked remains false by design",
  "Install evidence packet locked remains false by design",
  "Dependency allowlist locked remains false by design",
  "Rollback plan locked remains false by design",
  "Workspace boundary locked remains false by design",
  "Install scope locked remains false by design",
  "Install approval record approved remains false by design",
  "Install plan approved remains false by design",
  "Worker install approved remains false by design",
  "Execution unlock approved remains false by design",
  "Overnight work authorized remains false by design",
  "Worker installed remains false by design",
  "Worker connected remains false by design",
  "Windows scheduler configured remains false by design",
  "Scheduled execution remains false by design",
  "Executable schedule count remains zero",
  "Future install requires signed owner approval record",
  "Future install requires signed install scope lock",
  "Future install requires signed workspace boundary",
  "Future install requires signed rollback plan",
  "Future install requires signed dependency allowlist",
  "Future install requires signed install evidence packet review",
  "Future install requires signed manual install gate review",
  "Future install requires exact workspace path boundary",
  "Future install requires allowed workspace path inventory",
  "Future install requires blocked workspace path inventory",
  "Future install requires dependency inventory boundary",
  "Future install requires package manager boundary",
  "Future install requires pinned dependency versions",
  "Future install requires dependency provenance evidence",
  "Future install requires environment variable boundary",
  "Future install requires secret handling boundary",
  "Future install requires rollback target",
  "Future install requires rollback trigger map",
  "Future install requires state restore boundary",
  "Future install requires rollback evidence target",
  "Future install requires install evidence target",
  "Future install requires worker health evidence target",
  "Future install requires install dry-run script review",
  "Future install requires install dry-run input inventory",
  "Future install requires install dry-run output evidence",
  "Future install requires install dry-run smoke test acknowledgement",
  "Future install requires evidence source inventory",
  "Future install requires evidence bundle manifest",
  "Future install requires validation evidence inventory",
  "Future install requires no-install mutation proof",
  "Future install requires manual command plan review",
  "Future install requires manual preinstall checklist review",
  "Future install requires scheduler hold acknowledgement",
  "Future install requires command allowlist acknowledgement",
  "Future install requires emergency stop acknowledgement",
  "Future install requires session lock acknowledgement",
  "Future install requires audit evidence acknowledgement",
  "Future install requires no-secret leakage acknowledgement",
  "Future install requires approval revocation path",
  "Future install requires out-of-scope dependency handling",
  "Future install requires owner re-approval for dependency changes",
  "Future install requires blocked dependency policy",
  "Future install requires dependency update policy",
  "Future install requires dependency vulnerability review policy",
  "Future install requires license review policy",
  "Future install requires transitive dependency review policy",
  "Future install requires dependency source trust policy",
  "Future install requires no-network install boundary",
  "Future install requires no-write install boundary",
  "Future install requires no-execution install boundary",
  "Future install requires simulated install checklist",
  "Future install requires simulated dependency checklist",
  "Future install requires simulated rollback checklist",
  "Future install requires simulated health checklist",
  "Future install requires install failure handling plan",
  "Future install requires install retry policy",
  "Future install requires install log redaction policy",
  "Future install requires install artifact boundary",
  "Future install requires install owner review queue",
  "Future install requires install blocker reporting",
  "Future install requires install risk rating",
  "Future install requires install pass/fail criteria",
  "Future install requires install smoke evidence plan",
  "Future install requires install no-secret check",
  "Future install requires install no-mutation proof",
  "Future install requires install no-network proof",
  "Future install requires install no-command proof",
  "No backend manual install gate service",
  "No authentication changes",
  "No approval signing in this phase",
  "No scope lock signing in this phase",
  "No workspace boundary signing in this phase",
  "No rollback plan signing in this phase",
  "No dependency allowlist signing in this phase",
  "No install evidence packet signing in this phase",
  "No manual install gate signing in this phase",
  "No approval persistence in this phase",
  "No scope lock persistence in this phase",
  "No workspace boundary persistence in this phase",
  "No rollback plan persistence in this phase",
  "No dependency allowlist persistence in this phase",
  "No install evidence packet persistence in this phase",
  "No manual install gate persistence in this phase",
  "No live worker connection",
  "No actual worker install",
  "No installer execution",
  "No dependency download",
  "No package install",
  "No package manager execution",
  "No dependency manifest mutation",
  "No lockfile mutation",
  "No workspace probing",
  "No filesystem scanning",
  "No path creation",
  "No path deletion",
  "No rollback execution",
  "No state restore execution",
  "No backup creation",
  "No network access",
  "No manual install execution",
  "No smoke test execution",
  "No artifact writes except local phase report",
  "Manual install gate requires evidence packet proof reference",
  "Manual install gate requires dependency allowlist proof reference",
  "Manual install gate requires rollback plan proof reference",
  "Manual install gate requires workspace boundary proof reference",
  "Manual install gate requires scope lock proof reference",
  "Manual install gate requires approval record proof reference",
  "Manual install gate requires install plan proof reference",
  "Manual install gate requires no-download proof reference",
  "Manual install gate requires no-install proof reference",
  "Manual install gate requires no-command proof reference",
  "Manual install gate requires no-network proof reference",
  "Manual install gate requires no-filesystem-mutation proof reference",
  "Manual install gate requires no-artifact-mutation proof reference",
  "Manual install gate requires no-secret-leakage proof reference",
  "Manual install gate requires owner review checklist",
  "Manual install gate requires blocker inventory",
  "Manual install gate requires final preinstall checklist",
  "Manual install gate requires manual command plan",
  "Manual install gate requires validation evidence inventory",
  "Manual install gate requires readiness non-approval statement",
  "Manual install gate cannot convert review into approval",
  "Manual install gate cannot mark evidence packet locked",
  "Manual install gate cannot mark dependency allowlist locked",
  "Manual install gate cannot mark rollback plan locked",
  "Manual install gate cannot mark workspace boundary locked",
  "Manual install gate cannot mark install scope locked",
  "Manual install gate cannot mark worker ready for install",
  "Manual install gate cannot approve worker installation",
  "Manual install gate cannot persist approval records",
  "Manual install gate cannot persist gate approvals",
  "Manual install gate remains frontend-only",
  "Manual install gate remains local-only",
  "Manual install gate remains private-app-only",
  "Manual install gate remains owner-review-only",
  "Manual install gate remains declarative-only",
  "Manual install gate remains read-only",
  "Manual install gate blocks report signing",
  "Manual install gate blocks gate promotion",
  "Manual install gate blocks install execution",
  "Manual install gate blocks self-approval",
  "Manual install gate safety control 001",
  "Manual install gate safety control 002",
  "Manual install gate safety control 003",
  "Manual install gate safety control 004",
  "Manual install gate safety control 005",
  "Manual install gate safety control 006",
  "Manual install gate safety control 007",
  "Manual install gate safety control 008",
  "Manual install gate safety control 009",
  "Manual install gate safety control 010",
  "Manual install gate safety control 011",
  "Manual install gate safety control 012",
  "Manual install gate safety control 013",
  "Manual install gate safety control 014",
  "Manual install gate safety control 015",
  "Manual install gate safety control 016",
  "Manual install gate safety control 017",
  "Manual install gate safety control 018",
  "Manual install gate safety control 019",
  "Manual install gate safety control 020",
  "Manual install gate safety control 021",
  "Manual install gate safety control 022",
  "Manual install gate safety control 023",
  "Manual install gate safety control 024",
  "Manual install gate safety control 025",
  "Manual install gate safety control 026",
  "Manual install gate safety control 027"
];

const defaultSummary = {
  "manualInstallGateId": "phase70_local_worker_manual_install_gate",
  "owner": "Tyler Wallace",
  "sourcePhase": "Phase 69 Local Worker Install Evidence Packet",
  "safeState": "manual-install-gate-only",
  "phase69InstallEvidencePacketReady": true,
  "phase68InstallDryRunReady": true,
  "ownerApprovalRequired": true,
  "manualReviewRequired": true,
  "explicitManualInstallGateRequired": true,
  "ownerInstallReviewRequired": true,
  "manualInstallCommandPlanRequired": true,
  "finalPreinstallChecklistRequired": true,
  "installRemainsBlockedRequired": true,
  "emergencyStopAcknowledgementRequired": true,
  "localWorkerReadyForInstall": false,
  "manualInstallGateLocked": false,
  "installEvidencePacketLocked": false,
  "dependencyAllowlistLocked": false,
  "rollbackPlanLocked": false,
  "workspaceBoundaryLocked": false,
  "installScopeLocked": false,
  "installApprovalRecordApproved": false,
  "installPlanApproved": false,
  "executionUnlockApproved": false,
  "overnightWorkAuthorized": false,
  "workerInstallApproved": false,
  "workerInstalled": false,
  "workerConnected": false,
  "windowsSchedulerConfigured": false,
  "scheduledExecutionAllowed": false,
  "executableScheduleCount": 0
};
const defaultBoundaries = {
  "localOnly": true,
  "privateAppOnly": true,
  "manualInstallGateOnly": true,
  "ownerReviewOnly": true,
  "declarativeOnly": true,
  "readOnly": true,
  "frontendOnly": true,
  "noBackendLogic": true,
  "noAuthentication": true,
  "manualInstallGateSigningAllowed": false,
  "installEvidencePacketSigningAllowed": false,
  "dependencyAllowlistSigningAllowed": false,
  "rollbackPlanSigningAllowed": false,
  "workspaceBoundarySigningAllowed": false,
  "scopeLockSigningAllowed": false,
  "approvalRecordSigningAllowed": false,
  "manualInstallExecutionAllowed": false,
  "evidencePacketExecutionAllowed": false,
  "dryRunExecutionAllowed": false,
  "smokeTestExecutionAllowed": false,
  "networkAccessAllowed": false,
  "artifactMutationAllowed": false,
  "dependencyDownloadAllowed": false,
  "packageInstallAllowed": false,
  "packageManagerExecutionAllowed": false,
  "dependencyManifestMutationAllowed": false,
  "lockfileMutationAllowed": false,
  "rollbackExecutionAllowed": false,
  "stateRestoreAllowed": false,
  "backupCreationAllowed": false,
  "executionUnlockAllowed": false,
  "overnightExecutionAllowed": false,
  "liveRunReportAllowed": false,
  "installerExecutionAllowed": false,
  "schedulerCreationAllowed": false,
  "schedulerMutationAllowed": false,
  "schedulerDeletionAllowed": false,
  "schedulerEnableDisableAllowed": false,
  "schedulerQueryAllowed": false,
  "powershellExecutionAllowed": false,
  "schtasksExecutionAllowed": false,
  "commandExecutionAllowed": false,
  "shellExecutionAllowed": false,
  "runnerConnectivityAllowed": false,
  "liveWorkerConnectionAllowed": false,
  "workerInstallAllowed": false,
  "workerConnectionAllowed": false,
  "workerSpawnAllowed": false,
  "taskExecutionAllowed": false,
  "healthPollingAllowed": false,
  "liveHeartbeatAllowed": false,
  "processInspectionAllowed": false,
  "workspaceProbeAllowed": false,
  "filesystemScanAllowed": false,
  "mutatesSource": false,
  "fileMutationAllowed": false,
  "filesystemMutationAllowed": false,
  "pathCreationAllowed": false,
  "pathDeletionAllowed": false,
  "recordPersistenceAllowed": false,
  "taskPersistenceAllowed": false,
  "morningPacketPersistenceAllowed": false,
  "readinessGatePersistenceAllowed": false,
  "unlockProposalPersistenceAllowed": false,
  "installPlanPersistenceAllowed": false,
  "installApprovalRecordPersistenceAllowed": false,
  "installScopeLockPersistenceAllowed": false,
  "workspaceBoundaryPersistenceAllowed": false,
  "rollbackPlanPersistenceAllowed": false,
  "dependencyAllowlistPersistenceAllowed": false,
  "installDryRunPersistenceAllowed": false,
  "installEvidencePacketPersistenceAllowed": false,
  "manualInstallGatePersistenceAllowed": false,
  "finalApprovalAllowed": false,
  "autoApprovalAllowed": false,
  "autoProcessingAllowed": false,
  "autoRouteAllowed": false,
  "autoMergeAllowed": false,
  "selfApprovalAllowed": false
};
const defaultRouting = {
  "suggestedQueue": "Tyler manual local worker install gate review",
  "reviewRequired": true,
  "ownerDecisionRequired": true,
  "manualInstallGateSigningAllowed": false,
  "installPlanApprovalAllowed": false,
  "workerInstallAllowed": false,
  "readinessUnlockAllowed": false,
  "overnightExecutionAllowed": false,
  "schedulerQueryAllowed": false,
  "workerConnectionAllowed": false,
  "workerSpawnAllowed": false,
  "taskExecutionAllowed": false,
  "commandExecutionAllowed": false,
  "shellExecutionAllowed": false,
  "autoRouteAllowed": false,
  "autoProcessingAllowed": false
};

function isSafeRelativePath(value) {
  return typeof value === "string" && value.length > 0 && !path.isAbsolute(value) && !value.split(/[\\/]+/).includes("..");
}
function checkFile(rootDir, relativePath, blockers) {
  if (!isSafeRelativePath(relativePath)) { blockers.push(`Declared path must be safe and relative: ${relativePath}`); return; }
  if (!fs.existsSync(path.join(rootDir, relativePath))) blockers.push(`Declared file missing: ${relativePath}`);
}
function requireTrue(value, name, blockers) { if (value !== true) blockers.push(`${name} must be true`); }
function requireFalse(value, name, blockers) { if (value !== false) blockers.push(`${name} must remain false`); }

export function createDefaultLocalWorkerManualInstallGateV1() {
  return {
    declaredPaths: [...declaredPaths],
    status: "manual-install-gate-ready",
    summary: { ...defaultSummary },
    boundaries: { ...defaultBoundaries },
    requirements: manualInstallGateRequirements.map((item) => ({ ...item })),
    fields: [...manualInstallGateFields],
    evidenceRequirements: [...evidenceRequirements],
    signals: [...manualInstallGateSignals],
    safetyGates: [...safetyGates],
    routing: { ...defaultRouting },
  };
}

export function inspectLocalWorkerManualInstallGateV1(config = createDefaultLocalWorkerManualInstallGateV1(), options = {}) {
  const rootDir = options.rootDir ?? process.cwd();
  const blockers = [];
  for (const relativePath of config.declaredPaths ?? []) checkFile(rootDir, relativePath, blockers);
  const packagePath = path.join(rootDir, "package.json");
  if (fs.existsSync(packagePath)) {
    const pkg = JSON.parse(fs.readFileSync(packagePath, "utf8"));
    if (pkg.scripts?.["phase70:demo"] !== "node scripts/run-local-worker-manual-install-gate-v1.mjs") blockers.push("package.json phase70:demo script is missing or incorrect");
    if (pkg.scripts?.["phase70:verify"] !== "npm run free-core:verify && npm run knowledge:verify && npm run phase69:demo && npm run phase70:demo") blockers.push("package.json phase70:verify script is missing or incorrect");
  } else blockers.push("package.json missing");
  const appPath = path.join(rootDir, "apps/operator-console/src/App.tsx");
  let appBindingCount = 0;
  if (fs.existsSync(appPath)) {
    const app = fs.readFileSync(appPath, "utf8");
    const bindings = [
      "localWorkerManualInstallGate.manualInstallGateSummary.owner",
      "localWorkerManualInstallGate.manualInstallGateRequirements.length",
      "localWorkerManualInstallGate.evidenceRequirements.length",
      "localWorkerManualInstallGate.boundaries.workerInstallAllowed",
      "localWorkerManualInstallGate.manualInstallGateSummary.manualInstallGateLocked",
    ];
    appBindingCount = bindings.filter((binding) => app.includes(binding)).length;
    for (const binding of bindings) if (!app.includes(binding)) blockers.push(`App binding missing: ${binding}`);
  } else blockers.push("App.tsx missing");
  const summary = config.summary ?? {};
  const boundaries = config.boundaries ?? {};
  requireTrue(summary.phase69InstallEvidencePacketReady, "phase69InstallEvidencePacketReady", blockers);
  requireTrue(summary.phase68InstallDryRunReady, "phase68InstallDryRunReady", blockers);
  requireTrue(summary.ownerApprovalRequired, "ownerApprovalRequired", blockers);
  requireTrue(summary.manualReviewRequired, "manualReviewRequired", blockers);
  requireTrue(summary.explicitManualInstallGateRequired, "explicitManualInstallGateRequired", blockers);
  requireTrue(summary.ownerInstallReviewRequired, "ownerInstallReviewRequired", blockers);
  requireTrue(summary.manualInstallCommandPlanRequired, "manualInstallCommandPlanRequired", blockers);
  requireTrue(summary.finalPreinstallChecklistRequired, "finalPreinstallChecklistRequired", blockers);
  requireTrue(summary.installRemainsBlockedRequired, "installRemainsBlockedRequired", blockers);
  requireTrue(summary.emergencyStopAcknowledgementRequired, "emergencyStopAcknowledgementRequired", blockers);
  for (const name of ["localWorkerReadyForInstall","manualInstallGateLocked","installEvidencePacketLocked","dependencyAllowlistLocked","rollbackPlanLocked","workspaceBoundaryLocked","installScopeLocked","installApprovalRecordApproved","installPlanApproved","executionUnlockApproved","overnightWorkAuthorized","workerInstallApproved","workerInstalled","workerConnected","windowsSchedulerConfigured","scheduledExecutionAllowed"]) requireFalse(summary[name], name, blockers);
  if (summary.executableScheduleCount !== 0) blockers.push("executableScheduleCount must remain zero");
  for (const name of ["manualInstallGateSigningAllowed","installEvidencePacketSigningAllowed","manualInstallExecutionAllowed","evidencePacketExecutionAllowed","dryRunExecutionAllowed","smokeTestExecutionAllowed","networkAccessAllowed","artifactMutationAllowed","dependencyDownloadAllowed","packageInstallAllowed","packageManagerExecutionAllowed","dependencyManifestMutationAllowed","lockfileMutationAllowed","installerExecutionAllowed","commandExecutionAllowed","shellExecutionAllowed","runnerConnectivityAllowed","workerInstallAllowed","workerConnectionAllowed","workerSpawnAllowed","taskExecutionAllowed","workspaceProbeAllowed","filesystemScanAllowed","filesystemMutationAllowed","pathCreationAllowed","pathDeletionAllowed","manualInstallGatePersistenceAllowed","finalApprovalAllowed","autoRouteAllowed","selfApprovalAllowed"]) requireFalse(boundaries[name], name, blockers);
  requireTrue(boundaries.localOnly, "localOnly", blockers);
  requireTrue(boundaries.privateAppOnly, "privateAppOnly", blockers);
  requireTrue(boundaries.manualInstallGateOnly, "manualInstallGateOnly", blockers);
  requireTrue(boundaries.ownerReviewOnly, "ownerReviewOnly", blockers);
  requireTrue(boundaries.declarativeOnly, "declarativeOnly", blockers);
  requireTrue(boundaries.readOnly, "readOnly", blockers);
  requireTrue(boundaries.frontendOnly, "frontendOnly", blockers);
  if ((config.declaredPaths ?? []).length !== 5) blockers.push("declaredFileCount must equal 5");
  if ((config.requirements ?? []).length !== 6) blockers.push("manualInstallGateRequirementCount must equal 6");
  if ((config.fields ?? []).length !== 8) blockers.push("manualInstallGateFieldCount must equal 8");
  if ((config.evidenceRequirements ?? []).length !== 6) blockers.push("manualInstallGateEvidenceCount must equal 6");
  if ((config.signals ?? []).length !== 8) blockers.push("manualInstallGateSignalCount must equal 8");
  if ((config.safetyGates ?? []).length !== 260) blockers.push("safetyGateCount must equal 260");
  const result = {
    ok: blockers.length === 0,
    status: blockers.length === 0 ? "passed" : "failed",
    localWorkerManualInstallGateStatus: config.status,
    validationFailedCount: blockers.length,
    blockers,
    declaredFileCount: (config.declaredPaths ?? []).length,
    manualInstallGateRequirementCount: (config.requirements ?? []).length,
    manualInstallGateFieldCount: (config.fields ?? []).length,
    manualInstallGateEvidenceCount: (config.evidenceRequirements ?? []).length,
    manualInstallGateSignalCount: (config.signals ?? []).length,
    safetyGateCount: (config.safetyGates ?? []).length,
    appBindingCount,
    ...summary,
    ...boundaries,
  };
  if (options.writeArtifacts) {
    const reportDir = path.join(rootDir, ".sera-local-worker-manual-install-gate");
    fs.mkdirSync(reportDir, { recursive: true });
    fs.writeFileSync(path.join(reportDir, "phase70-local-worker-manual-install-gate-status.json"), JSON.stringify(result, null, 2), "utf8");
    fs.writeFileSync(path.join(reportDir, "phase70-local-worker-manual-install-gate-status.md"), `# Phase 70 Local Worker Manual Install Gate v1\n\nStatus: ${result.status}\n\nValidation failed count: ${result.validationFailedCount}\n`, "utf8");
  }
  return result;
}
